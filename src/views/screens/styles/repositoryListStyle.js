import {StyleSheet} from "react-native";

export default styles = StyleSheet.create({
    base: {
        flex: 1
    },
    emptyList: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
})